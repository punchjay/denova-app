import { fadeIn, rotate } from '../Components/keyFrames.style'

it('exports fadeIn keyframe', () => {
  expect(fadeIn).toBeDefined()
})

it('exports rotate keyframe', () => {
  expect(rotate).toBeDefined()
})
